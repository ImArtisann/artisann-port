---
import type { ResponsiveValue } from "./shared.ts";

interface Props {
  value: ResponsiveValue;
}

const { value } = Astro.props;
---

<span class="lg:hidden">{value.mobile}</span>
<span class="hidden lg:inline">{value.desktop}</span>
